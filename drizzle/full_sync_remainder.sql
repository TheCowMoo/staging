-- ============================================================
-- Five Stones Safeguard — full_sync_remainder.sql (DEPRECATED — NO-OP)
-- ============================================================
-- This file intentionally does nothing.
--
-- `full_sync.sql` was regenerated from `drizzle/schema.ts` by
-- `scripts/dump-full-sync.ts` and now contains the COMPLETE, authoritative
-- schema for all 45 tables. It is idempotent (CREATE TABLE IF NOT EXISTS),
-- so running only `full_sync.sql` is sufficient — for fresh installs AND
-- existing databases.
--
-- This file is kept as a safe no-op so existing deployment scripts that
-- reference it keep working unchanged.

SELECT 'full_sync_remainder.sql is deprecated — full_sync.sql now contains the complete schema.' AS result;
